# PostCSS XSS via unescaped </style> (bundled inside Next.js internals)
# npm audit fix --force would downgrade Next.js to 9.x — not actionable.
# Suppressed until Next.js ships with postcss >= 8.5.10.
GHSA-qx2v-qp2m-jg93
CVE-2023-44270
